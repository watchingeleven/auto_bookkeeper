name: auto_bookkeeper
description: 自动记账 - 支付宝/微信支付通知自动识别记账
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  path: ^1.9.1
  provider: ^6.1.5
  firebase_core: ^3.12.1
  cloud_firestore: ^5.6.8
  firebase_auth: ^5.5.2
  intl: ^0.20.2
  fl_chart: ^0.70.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
